# .env.pi - Configuration for Raspberry Pi Clients
# Pis don't need blockchain credentials, just place in project root as .env

# Pi clients need minimal configuration
# All blockchain/IPFS operations happen on server

# This file can be empty or have basic configuration if needed
# For now, just an empty template
